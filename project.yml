name: MobileHarmonica

options:
  bundleIdPrefix: com.vladyslavshcherbakov
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "5.9"
    TARGETED_DEVICE_FAMILY: "1"
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: ""

packages:
  HarmonicaCore:
    path: Core

targets:
  MobileHarmonica:
    type: application
    platform: iOS
    sources:
      - path: Apps/iOS
        excludes:
          - Tests
          - Scores
      - path: Resources/Samples
        type: folder
      - path: Apps/iOS/Scores
        type: folder
    dependencies:
      - package: HarmonicaCore
        product: HarmonicaCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.vladyslavshcherbakov.mobileharmonica
        PRODUCT_NAME: MobileHarmonica
        GENERATE_INFOPLIST_FILE: NO
    info:
      path: Apps/iOS/Info.plist
      properties:
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight

  MobileHarmonicaTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: Apps/iOS/Tests
    dependencies:
      - target: MobileHarmonica
      - package: HarmonicaCore
        product: HarmonicaCoreTestSupport
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES

schemes:
  MobileHarmonica:
    build:
      targets:
        MobileHarmonica: all
        MobileHarmonicaTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - MobileHarmonicaTests
        - package: HarmonicaCore/HarmonicaCoreTests
    archive:
      config: Release
